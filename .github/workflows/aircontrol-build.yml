name: AirControl APK build
on:
  push:
    branches: [aircontrol-build-temp]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        working-directory: aircontrol
        run: gradle --no-daemon assembleDebug
      - name: Collect APK
        run: cp aircontrol/app/build/outputs/apk/debug/app-debug.apk AirControlV1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: AirControlV1-apk
          path: AirControlV1.apk
          if-no-files-found: error
