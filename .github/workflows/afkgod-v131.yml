name: AFK God Android v1.3.1 temporary build

on:
  push:
    branches:
      - temp-afkgod-apk-v131
    paths:
      - .github/workflows/afkgod-v131.yml

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 36
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'
      - name: Restore project
        run: |
          mkdir -p /tmp/afkgod
          base64 -d tmp-afkgod-v131/android-source.tgz.b64 | tar -xz -C /tmp/afkgod
      - name: Build APK
        working-directory: /tmp/afkgod
        run: gradle --no-daemon clean assembleDebug
      - name: Prepare artifact
        run: cp /tmp/afkgod/app/build/outputs/apk/debug/app-debug.apk /tmp/AFKGodNode-Android-v1.3.1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: AFKGodNode-Android-v1.3.1
          path: /tmp/AFKGodNode-Android-v1.3.1.apk
          if-no-files-found: error
