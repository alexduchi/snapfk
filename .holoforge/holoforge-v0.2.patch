diff -ruN holoforge-original/app/build.gradle holoforge-modified/app/build.gradle
--- holoforge-original/app/build.gradle	2026-07-18 20:42:44.314034786 +0000
+++ holoforge-modified/app/build.gradle	2026-07-18 20:41:30.257583393 +0000
@@ -10,8 +10,8 @@
         applicationId 'com.neo.holoforge'
         minSdk 24
         targetSdk 35
-        versionCode 1
-        versionName '0.1.0'
+        versionCode 2
+        versionName '0.2.0'
     }
 
     compileOptions {
diff -ruN holoforge-original/app/src/main/AndroidManifest.xml holoforge-modified/app/src/main/AndroidManifest.xml
--- holoforge-original/app/src/main/AndroidManifest.xml	2026-07-18 20:42:44.313777356 +0000
+++ holoforge-modified/app/src/main/AndroidManifest.xml	2026-07-18 20:41:30.257409484 +0000
@@ -1,6 +1,6 @@
 <manifest xmlns:android="http://schemas.android.com/apk/res/android">
     <uses-permission android:name="android.permission.CAMERA" />
-    <uses-feature android:name="android.hardware.camera.ar" android:required="true" />
+    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
     <uses-feature android:glEsVersion="0x00020000" android:required="true" />
 
     <application
@@ -8,7 +8,7 @@
         android:label="HoloForge AR"
         android:theme="@style/AppTheme"
         android:usesCleartextTraffic="false">
-        <meta-data android:name="com.google.ar.core" android:value="required" />
+        <meta-data android:name="com.google.ar.core" android:value="optional" />
         <activity
             android:name=".MainActivity"
             android:configChanges="orientation|screenSize|keyboardHidden"
diff -ruN holoforge-original/app/src/main/java/com/neo/holoforge/HandTracker.java holoforge-modified/app/src/main/java/com/neo/holoforge/HandTracker.java
--- holoforge-original/app/src/main/java/com/neo/holoforge/HandTracker.java	2026-07-18 20:42:44.313561275 +0000
+++ holoforge-modified/app/src/main/java/com/neo/holoforge/HandTracker.java	2026-07-18 20:41:30.256970926 +0000
@@ -82,7 +82,7 @@
                                 .setMinTrackingConfidence(0.50f)
                                 .build();
                 landmarker = HandLandmarker.createFromOptions(context, options);
-            } catch (RuntimeException ex) {
+            } catch (Throwable ex) {
                 Log.e("HoloForge", "Hand tracker init failed", ex);
                 listener.onHandError("Suivi des mains indisponible");
             }
@@ -147,7 +147,7 @@
             }
             rotated.recycle();
             listener.onHands(Collections.unmodifiableList(poses), SystemClock.uptimeMillis() - start);
-        } catch (Exception ex) {
+        } catch (Throwable ex) {
             Log.w("HoloForge", "Hand frame failed", ex);
             listener.onHandError("Suivi limité");
         } finally {
diff -ruN holoforge-original/app/src/main/java/com/neo/holoforge/MainActivity.java holoforge-modified/app/src/main/java/com/neo/holoforge/MainActivity.java
--- holoforge-original/app/src/main/java/com/neo/holoforge/MainActivity.java	2026-07-18 20:42:44.313015248 +0000
+++ holoforge-modified/app/src/main/java/com/neo/holoforge/MainActivity.java	2026-07-18 20:41:40.027118115 +0000
@@ -42,6 +42,9 @@
     private HoloRenderer renderer;
     private HandTracker handTracker;
     private Session session;
+    private boolean activityResumed;
+    private boolean sessionResumed;
+    private boolean surfaceResumed;
     private boolean installRequested;
     private TextView arStatus;
     private TextView modeChip;
@@ -57,8 +60,6 @@
 
         renderer = new HoloRenderer(this);
         surfaceView = new HoloSurfaceView(this, renderer);
-        handTracker = new HandTracker(this, renderer);
-        renderer.setHandTracker(handTracker);
 
         root = new FrameLayout(this);
         root.setBackgroundColor(Color.BLACK);
@@ -233,6 +234,7 @@
             session.configure(config);
             renderer.setSession(session);
             updateRotation();
+            resumeArIfReady();
         } catch (UnavailableArcoreNotInstalledException | UnavailableUserDeclinedInstallationException ex) {
             showFatal("ARCore n’est pas installé.");
         } catch (UnavailableApkTooOldException ex) {
@@ -241,7 +243,8 @@
             showFatal("Cette version de HoloForge doit être mise à jour.");
         } catch (UnavailableDeviceNotCompatibleException ex) {
             showFatal("Ce téléphone n’est pas compatible ARCore.");
-        } catch (Exception ex) {
+        } catch (Throwable ex) {
+            android.util.Log.e("HoloForge", "ARCore startup failed", ex);
             showFatal("Impossible de démarrer la réalité augmentée.");
         }
     }
@@ -250,44 +253,92 @@
         onArStatus(message);
         new AlertDialog.Builder(this)
                 .setTitle("HoloForge AR")
-                .setMessage(message)
-                .setPositiveButton("Fermer", (dialog, which) -> finish())
+                .setMessage(message + "\n\nL'application reste ouverte pour afficher ce diagnostic.")
+                .setPositiveButton("Réessayer", (dialog, which) -> {
+                    closeSessionSafely();
+                    createSessionIfNeeded();
+                })
+                .setNegativeButton("Fermer", (dialog, which) -> finish())
+                .setCancelable(false)
                 .show();
     }
 
+    private void resumeArIfReady() {
+        if (!activityResumed || session == null) return;
+        try {
+            if (!sessionResumed) {
+                session.resume();
+                sessionResumed = true;
+            }
+            if (!surfaceResumed) {
+                surfaceView.onResume();
+                surfaceResumed = true;
+            }
+            updateRotation();
+            startHandTrackingSafely();
+        } catch (CameraNotAvailableException ex) {
+            showFatal("La caméra est utilisée par une autre application.");
+        } catch (Throwable ex) {
+            android.util.Log.e("HoloForge", "AR resume failed", ex);
+            showFatal("Impossible de démarrer la session AR sur ce téléphone.");
+        }
+    }
+
+    private void startHandTrackingSafely() {
+        if (handTracker != null) return;
+        try {
+            handTracker = new HandTracker(this, renderer);
+            renderer.setHandTracker(handTracker);
+        } catch (Throwable ex) {
+            android.util.Log.e("HoloForge", "Hand tracking unavailable", ex);
+            onArStatus("ARCore actif · suivi des mains indisponible · tactile disponible");
+        }
+    }
+
+    private void closeSessionSafely() {
+        try {
+            if (surfaceResumed) surfaceView.onPause();
+        } catch (Throwable ignored) { }
+        surfaceResumed = false;
+        try {
+            if (session != null && sessionResumed) session.pause();
+        } catch (Throwable ignored) { }
+        sessionResumed = false;
+        try {
+            if (session != null) session.close();
+        } catch (Throwable ignored) { }
+        session = null;
+        renderer.setSession(null);
+    }
+
     @Override
     protected void onResume() {
         super.onResume();
+        activityResumed = true;
         enterImmersiveMode();
         if (!privacyShown) requestCameraAndStart();
-        if (session != null) {
-            try {
-                session.resume();
-                surfaceView.onResume();
-                updateRotation();
-            } catch (CameraNotAvailableException ex) {
-                showFatal("La caméra est utilisée par une autre application.");
-            }
-        }
+        resumeArIfReady();
     }
 
     @Override
     protected void onPause() {
+        activityResumed = false;
+        try {
+            if (surfaceResumed) surfaceView.onPause();
+        } catch (Throwable ignored) { }
+        surfaceResumed = false;
+        try {
+            if (session != null && sessionResumed) session.pause();
+        } catch (Throwable ignored) { }
+        sessionResumed = false;
         super.onPause();
-        if (session != null) {
-            surfaceView.onPause();
-            session.pause();
-        }
     }
 
     @Override
     protected void onDestroy() {
         super.onDestroy();
         if (handTracker != null) handTracker.close();
-        if (session != null) {
-            session.close();
-            session = null;
-        }
+        closeSessionSafely();
     }
 
     @Override
@@ -331,7 +382,7 @@
         if (requestCode != CAMERA_PERMISSION_REQUEST) return;
         if (grantResults.length > 0 && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
             createSessionIfNeeded();
-            onResume();
+            resumeArIfReady();
         } else {
             new AlertDialog.Builder(this)
                     .setTitle("Caméra requise")
